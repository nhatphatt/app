{
  "name": "minitake-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute minitake --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute minitake --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "hono-rate-limiter": "^0.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "wrangler": "^3.99.0",
    "typescript": "^5.7.0"
  }
}
