[phases.setup]
nixPkgs = ["python310", "gcc"]

[phases.install]
cmds = [
  "cd backend && python -m pip install --user --break-system-packages -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "cd backend && python -m uvicorn server:app --host 0.0.0.0 --port $PORT"
