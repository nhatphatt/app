[phases.setup]
nixPkgs = ["python310", "gcc", "pip"]

[phases.install]
cmds = [
  "cd backend && pip install --user -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "cd backend && python -m uvicorn server:app --host 0.0.0.0 --port $PORT"
